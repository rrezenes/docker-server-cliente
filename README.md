Configuração Docker para rodar aplicãção cliente com Nginx, MySQL, Redis and PHP7-FPM.
* Run `git clone https://www.github.com/rrezenes/cliente.git`
* Run `docker-compose up -d --build`
* Run `bash setup.sh`